<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Android 7 clamps the per-app heap to dalvik.vm.heapgrowthlimit — 48MB on the API 24
         emulator. The backend is jOOQ + RxJava + a native SQLite over a source import that
         pre-allocates ~34k content rows from versification, and reading those back through
         ContentDao.fetchByCollectionId (select(), no limit) exhausted it. largeHeap asks for
         dalvik.vm.heapsize instead; it raises the ceiling rather than reducing demand. -->
    <application
        android:name=".OratureApplication"
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
